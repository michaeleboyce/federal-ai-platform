# Downloaded original documents (~64 MB of PDFs/HTML, incl. documents/_governing/)
# are kept local-only, not versioned in git. The catalog in documents.csv records
# every document's source URL and local_path so the archive can be re-fetched.
documents/
